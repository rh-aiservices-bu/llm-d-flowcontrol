{{- if and (eq .Values.deploymentMethod "llmd") (eq .Values.llmd.flowControl true) }}
{{- range .Values.serviceAccounts }}
---
kind: Namespace
apiVersion: v1
metadata:
  name: {{ .namespace }}
  labels:
    app: llmd-demo
{{- end }}
{{- end }}
